# Resume Analyzer — Python Dependencies
# Install with:  pip install -r requirements_analyzer.txt

# ── Core (required) ────────────────────────────────
rapidfuzz>=3.6.0         # fuzzy matching (Stage 3)
scikit-learn>=1.3.0      # TF-IDF cosine similarity (experience matching)

# ── NLP — spaCy (recommended, not required) ────────
# spaCy gives Stage 2 NLP noun-chunk extraction.
# After pip install, also run:
#   python -m spacy download en_core_web_sm
spacy>=3.7.0

# ── Semantic similarity (optional, best accuracy) ──
# Uncomment for GPU/CPU sentence-transformer similarity
# sentence-transformers>=2.6.0

# ── FastAPI server (optional) ──────────────────────
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.0.0
